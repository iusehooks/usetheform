{
  "name": "usetheform-workspaces",
  "version": "1.0.0",
  "description": "React library for composing declarative forms in React and managing their state.",
  "repository": "github:iusehooks/usetheform",
  "homepage": "https://iusehooks.github.io/usetheform",
  "workspaces": [
    "workspaces/*"
  ],
  "private": true,
  "scripts": {
    "docz:build": "npm run docz:build --workspace docs",
    "docz:serve": "npm run docz:serve --workspace docs",
    "docz:dev": "npm run docz:dev --workspace docs",
    "build": "npm run build --workspace usetheform",
    "build:dev": "npm run build:dev --workspace usetheform",
    "lint": "npm run lint --workspaces --if-present",
    "pretest": "npm run lint",
    "test": "cross-env NODE_ENV=test npm test --workspaces --if-present --",
    "test:watch": "npm run test -- --watch",
    "test:cov": "npm test -- --coverage",
    "coveralls": "npm run test:cov && cat ./coverage/lcov.info | coveralls",
    "dev": "rimraf dev coverage && concurrently \"node ./build_config/dev.js\" \"browser-sync start --server examples --ss dev --files dev --files examples\"",
    "cy:open": "npx cypress open",
    "cy:run": "npx cypress run --headless",
    "cy:server:dev": "rimraf dev build coverage && concurrently \"npm run build:dev\" \"browser-sync start --no-ui --no-ghost-mode --no-open --port 3000 --server cypress/server --ss  workspaces/usetheform/dev --files workspaces/usetheform/dev --files cypress/server\"",
    "cy:server": "rimraf dev build coverage && node ./build_config/build.js && browser-sync start --no-ui --no-open --no-ghost-mode --port 3000 --server cypress/server --ss build/umd"
  },
  "author": "Antonio Pangallo (https://github.com/antoniopangallo)",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^7.0.0-bridge.0",
    "concurrently": "^5.3.0",
    "coveralls": "^3.1.0",
    "cross-env": "^7.0.3",
    "cypress": "^11.2.0",
    "cypress-file-upload": "5.0.8",
    "eslint": "^7.15.0",
    "eslint-config-prettier": "^7.0.0",
    "eslint-plugin-flowtype": "^5.2.0",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-prettier": "^3.3.0",
    "eslint-plugin-react": "^7.21.5",
    "husky": "^4.3.6",
    "prettier": "^2.2.1",
    "rimraf": "^3.0.2"
  },
  "resolutions": {
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "husky": {
    "hooks": {
      "pre-push": "npm test"
    }
  },
  "dependencies": {
    "@testing-library/react": "16.3.0",
    "@testing-library/user-event": "14.6.1",
    "jest": "29.7.0",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  }
}
